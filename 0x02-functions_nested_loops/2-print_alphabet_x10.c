#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */

int main(void)
{
	char i, j, a, b, c, d, e, f, g, h;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');
	for (j = 'a'; j <= 'z'; j++)
	{
		putchar(j);
	}
	putchar('\n');
	for (a = 'a'; a <= 'z'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	for (b = 'a'; b <= 'z'; b++)
	{
		putchar(b);
	}
	putchar('\n');
	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}putchar('\n');
	for (d = 'a'; d <= 'z'; d++)
	{
		putchar(d);
	}
	putchar('\n');
	for (e = 'a'; e <= 'z'; e++)
	{
		putchar(e);
	}
	putchar('\n');
	for (f = 'a'; f <= 'z'; f++)
	{
		putchar(f);
	}
	putchar('\n');
	for (g = 'a'; g <= 'z'; g++)
	{
		putchar(g);
	}
	putchar('\n');
	for (h = 'a'; h <= 'z'; h++)
	{
		putchar(h);
	}
	putchar('\n');
	return (0);

}
