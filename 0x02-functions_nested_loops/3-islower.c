#include "main.h"
#include <stdio.h>

/**
 * main - checks if a letter is lowercase
 *
 * Return - Always 0
 */

int main(void)
{
	int var = 'C';

	if (islower(var))
	{
		return (1);
	}
	else 
	{
		return (0);
	}
	return (0);
}
